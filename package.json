{
  "name": "marbleapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @marble/web",
    "build": "npm run build --workspace @marble/web",
    "preview": "npm run preview --workspace @marble/web",
    "test": "npm run test --workspace @marble/core && npm run test --workspace @marble/web && npm run test --workspace @marble/mcp-inventory && npm run test --workspace @marble/mcp-posthog && npm run test --workspace @marble/cli",
    "typecheck": "npm run typecheck --workspace @marble/core && npm run typecheck --workspace @marble/web && npm run typecheck --workspace @marble/mcp-inventory && npm run typecheck --workspace @marble/mcp-posthog && npm run typecheck --workspace @marble/cli",
    "pre-deploy": "node scripts/pre-deploy.mjs",
    "chat": "npm run chat --workspace @marble/cli --"
  },
  "engines": {
    "node": ">=22.5"
  }
}
